'use strict';

/**
 * program-investment service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::program-investment.program-investment');
