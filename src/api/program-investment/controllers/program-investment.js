'use strict';

/**
 * program-investment controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::program-investment.program-investment');
